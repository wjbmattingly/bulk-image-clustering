# install with `conda env create -f bulk-image.yaml`

name: bulk-image
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - docarray
  - timm
  - pandas
  - bokeh
  - umap-learn
  - hdbscan
  - joblib==1.1.0
